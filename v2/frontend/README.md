## v2 前端（uni-app）

### 打开方式
- 用 HBuilderX 打开本目录：`v2/frontend`

### 运行（无需小程序 AppID，先跑 H5）
- HBuilderX 顶部：运行 -> 运行到浏览器（H5）

### 后端地址
默认已配置为你的 Render：
- `https://my-football-stats.onrender.com`

如需切换，修改 `utils/config.js` 的 `API_BASE_URL`。

